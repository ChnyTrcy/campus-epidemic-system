<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.chnytrcy.campusepidemicsystem.mapper.TeacherMapper">

  <select id="queryTeacherPage"
          resultType="xyz.chnytrcy.campusepidemicsystem.model.entity.Teacher">
    SELECT
      id,
      code,
      name,
      dept_name AS deptName,
      phone,
      sex,
      id_card AS idCard,
      address,
      epidemic_mark AS epidemicMark
    FROM
      teacher
    WHERE
      is_delete = 0
      <if test="keyword != null and keyword != ''">
        <choose>
          <when test="wordType == 0">
            AND code LIKE CONCAT(#{keyword},'%')
          </when>
          <when test="wordType == 1">
            AND name LIKE CONCAT(#{keyword},'%')
          </when>
          <when test="wordType == 2">
            AND dept_name LIKE CONCAT(#{keyword},'%')
          </when>
        </choose>
      </if>

  </select>
  <select id="queryEpidemicPerson"
          resultType="xyz.chnytrcy.campusepidemicsystem.model.entity.Teacher">
    SELECT
      id,
      code,
      name,
      phone,
      sex,
      id_card AS idCard,
      address
    FROM
      teacher
    WHERE
      is_delete = 0 AND epidemic_mark = 1 AND dept_code = #{deptCode}
      <if test="keyword != null and keyword != ''">
        <if test="wordType == 1">
          AND name LIKE CONCAT(#{keyword},'%')
        </if>
        <if test="wordType == 2">
          AND code LIKE CONCAT(#{keyword},'%')
        </if>
      </if>
  </select>

</mapper>